
# Manipuleren tentamendata ------------------------------------------------

if (nrv == 1) {
  ll <- parse(file = "MC_toetsen/Analysescripts/Heranalyse Toetsanalyse 1 versie stap 1.R") 
}

if (nrv == 2) {
  ll <- parse(file = "MC_toetsen/Analysescripts/Heranalyse Toetsanalyse 2 versies stap 1.R") 
}

if (nrv == 3) {
  ll <- parse(file = "MC_toetsen/Analysescripts/Heranalyse Toetsanalyse 3 versies stap 1.R") 
}

if (nrv == 4) {
  ll <- parse(file = "MC_toetsen/Analysescripts/Heranalyse Toetsanalyse 4 versies stap 1.R") 
}

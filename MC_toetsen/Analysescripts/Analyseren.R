
# Analyseren --------------------------------------------------------------

if (nrv == 1) {
  ll <- parse(file = "MC_toetsen/Analysescripts/Heranalyse Toetsanalyse 1 versie stap 2.R")
}

if (nrv > 1) {
  ll <- parse(file = "MC_toetsen/Analysescripts/Heranalyse Toetsanalyse meerdere versies stap 2.R") 
}